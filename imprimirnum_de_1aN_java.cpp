#include <stdio.h>
#include <stdlib.h>

// Faça um algoritmo que imprima todos os números inteiros de 1 a N (fornecido
//pelo usuário). 

   main(){
      
      int n,i;
      
      printf ("Digite um numero: ");
       scanf ("%d", &n) ;
      
      for (i = 1; i <= n; i++){
      	  printf("%d \n",i);
	  }
        
   }
